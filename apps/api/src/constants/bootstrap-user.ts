/**
 * Synthetic user id for instance-scoped API keys (M1).
 * Must match `scripts/create-dev-api-key.ts` and admin rotation scope.
 */
export const API_KEY_INSTANCE_USER_ID = "app-default-bootstrap";
