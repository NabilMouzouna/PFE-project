/** HttpOnly cookie: operator access JWT (from API login). */
export const DASHBOARD_ACCESS_COOKIE = "appbase_dashboard_access";

/**
 * HttpOnly cookie: copy of API `appbase_session` for server-side logout against `apps/api`.
 */
export const DASHBOARD_API_SESSION_COOKIE = "appbase_dashboard_api_session";
